1. Copy booster and booster_watchdog to /bin.
2. Copy librt.so.0 to /lib if it is not already present.
3. Start /bin/booster_watchdog from init script after all main processes.
   By default it will run in foreground; to run as a daemon, set environment variable BOOSTER_WATCHDOG_DAEMON=1 upon invocation.
