AC_INIT(netstat-nat.c)
AM_INIT_AUTOMAKE(netstat-nat,1.4.10)
AC_PROG_CC
AC_PROG_INSTALL
AC_PREFIX_DEFAULT(/usr)


LDFLAGS="$LDFLAGS";
CFLAGS="$CFLAGS"
LIBS="$LIBS"

AC_HEADER_STDC
AC_CHECK_HEADERS(stdio.h stdlib.h string.h netdb.h unistd.h arpa/inet.h errno.h strings.h net/if.h sys/ioctl.h)

AC_OUTPUT([Makefile])
