##
## Temporary flags for static Teflon builds
##

#CODE_GEN_STYLE = STATIC
#OTHER_LDFLAGS = -nostdlib /lib/crt0.o
# Need -linfo twice to resolve circular dependency on getprotobyname
#OTHER_LIBS = -linfo -lrpc -linfo -lc -lm

CHFLAGS = true		# until we really have chflags on our system
